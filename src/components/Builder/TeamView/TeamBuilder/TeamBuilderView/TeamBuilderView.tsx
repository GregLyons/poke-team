import { Filters } from "../../../../App";

type TeamBuilderViewProps = {
  filters: Filters
}

const TeamBuilderView = ({
  filters,
}: TeamBuilderViewProps) => {
  return (
    <div className="team-builder__view-wrapper">
      View
    </div>
  )
}

export default TeamBuilderView;