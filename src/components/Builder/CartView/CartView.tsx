import {
  Cart,
} from "../../App";
import CartViewGroup from "./CartViewGroup";

type CartViewProps = {
  cart: Cart
}

const CartView = ({
  cart,
}: CartViewProps) => {
  return (
    <>
      yo
    </>
  )
}

export default CartView;