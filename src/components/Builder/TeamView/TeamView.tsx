import { Team } from "../../../hooks/App/Team";

type TeamViewProps ={
  team: Team
}

const TeamView = ({

}: TeamViewProps) => {
  return (
    <div className="team-view__wrapper">
      yo
    </div>
  )
}

export default TeamView;